const failures = ["misclick", "falling down" , "skateboerd accident" , "titktok fail"];
//loop through this array using map print out each element


 // in one line 
 // failures.map((failures , index)=> console.log (failures, index));

  const newFailures = failures.map((failure ) => {
     return failure;
});

console.log(newFailures);



// 